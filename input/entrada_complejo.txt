// Demuestra todos los tokens del lexer_complejo

// IF + ELSE con operadores relacionales EQ, LT, GT
if (x == 0) {
    y = 1;
} else {
    y = 0;
}

// WHILE con LEQ
while (i <= 10) {
    total = total + i;
    i = i + 1;
}

// Aritmetica completa: PLUS MINUS TIMES DIV
resultado = a + b * c - d / e;

// Comparaciones con GT y LT
if (max > min) {
    diff = max - min;
}

// COMMA en llamada simulada (lista de argumentos)
z = f + g, h;

// RETURN con expresion compuesta
return resultado;